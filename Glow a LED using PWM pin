// Blink an LED

int ledPin1 = 13; //PWM pins
int ledPin2 = 3; // PWM pins
void setup() {
 pinMode(ledPin1, OUTPUT);
 pinMode(ledPin2, OUTPUT);
}
void loop() {
 digitalWrite(ledPin1, 255); 

 digitalWrite(ledPin2, 100); 
}
